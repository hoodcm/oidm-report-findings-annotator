{
  "name": "oidm-report-findings-annotator",
  "version": "0.0.0",
  "private": true,
  "description": "Browser-based radiology report annotation tool. Pure-static SPA; devDeps are test-only.",
  "scripts": {
    "test": "node tests/run.js",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "dexie": "^4.0.10",
    "fake-indexeddb": "^6.0.0"
  }
}
